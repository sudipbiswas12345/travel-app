export default function Topbar() {
  return (
    <header className="h-16 bg-gray-400 shadow flex items-center px-6 sticky top-0 z-50">
      <h1 className="font-semibold text-lg">Admin Dashboard</h1>
    </header>
  );
}
